# Simple Object-Oriented Programming (OOP)Objective

- Create a `Person` class with properties and methods.
- Define properties such as `Name` and `Age`.
- Implement a method `Introduce()` that prints a personalized greeting.
- Instantiate a few `Person` objects in your `Main` method and call `Introduce()` on

## Output

```txt
Hi This is Alice and my age is 20
Hi This is Bob and my age is 21
Hi This is Charlie and my age is 18
```
